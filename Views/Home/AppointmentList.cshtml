@{
    ViewData["Title"] = "Appointment List";
}

@model List<Appointment>

<table class="table table-bordered table-striped">
    <thead class="font-weight-bold">
        <tr>
            <td>Date</td>
            <td>Group Name</td>
            <td>Group Size</td>
            <td>Email</td>
            <td>Phone</td>
            <td>Time</td>
        </tr>
    </thead>

    <tbody>
        @foreach (var x in Model)
        {
            <tr>
                <td>Date Placeholder</td>
                <td>@x.GroupName</td>
                <td>@x.GroupSize</td>
                <td>@x.EmailAddress</td>
                <td>@x.PhoneNumber</td>
                <td>@x.TimeSlot</td>
                <td><a asp-controller="Home" asp-action="Edit" asp-route-movieEntryID="@x.AppointmentId">Edit</a></td>
                <td><a asp-controller="Home" asp-action="Delete" asp-route-movieEntryID="@x.AppointmentId">Delete</a></td>
            </tr>
        }
    </tbody>
</table>